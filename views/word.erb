<h3>Here are the definitions for this word:</h3>

<ul>
  <% @word.entry().each() do |definition| %>
    <li><%= definition.define() %></li>
  <% end %>
</ul>

<a href="/words/<%= @word.id()%>/definitions/new">Add a definition</a>
<a href="/">Return to Main Menu</a>
